NEXT_PUBLIC_DEMO_MODE=false

# Optional. Without it the app falls back to the built-in template engine.
# Free key (no card): https://aistudio.google.com/apikey
GEMINI_API_KEY=
# Defaults to gemini-2.5-flash.
GEMINI_MODEL=
